version: 0.2

phases:
  pre_build:
    commands:
      - cd ./hello-app
      - echo Installing python packages...
      - python -m venv .venv
      - . .venv/bin/activate
      - pip install -r requirements.txt
  build:
    commands:
      - echo run tests...
      - python -m pytest
